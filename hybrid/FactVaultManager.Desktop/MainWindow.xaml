<Window x:Class="FactVaultManager.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FactVaultManager Hybrid" Height="620" Width="980"
        MinHeight="520" MinWidth="820"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="FactVaultManager Hybrid" FontSize="26" FontWeight="SemiBold" />
            <TextBlock Text="C# desktop shell with the existing Python production engine" Margin="0,4,0,0" Opacity="0.7" />
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Python worker" FontWeight="SemiBold" />
                    <TextBlock x:Name="WorkerStatusText" Text="Not connected" Margin="0,4,0,0" />
                    <TextBlock x:Name="RepositoryText" Text="Repository: locating..." Margin="0,4,0,0" Opacity="0.7" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="ConnectWorkerButton" Content="Connect Worker" Padding="16,8" Margin="0,0,8,0" Click="ConnectWorker_Click" />
                    <Button x:Name="LaunchLegacyButton" Content="Open Current Python App" Padding="16,8" Margin="0,0,8,0" Click="LaunchLegacy_Click" />
                    <Button x:Name="StopLegacyButton" Content="Stop Python App" Padding="16,8" Click="StopLegacy_Click" IsEnabled="False" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Margin="0,0,0,8">
                <TextBlock Text="Hybrid log" FontWeight="SemiBold" DockPanel.Dock="Left" />
                <Button Content="Clear" Padding="12,5" DockPanel.Dock="Right" Click="ClearLog_Click" />
            </DockPanel>
            <TextBox x:Name="LogTextBox"
                     Grid.Row="1"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="13"
                     Padding="10" />
        </Grid>
    </Grid>
</Window>
