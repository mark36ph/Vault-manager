<Window x:Class="FactVaultManager.Desktop.ProductionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FactVaultManager Hybrid" Height="760" Width="1040"
        MinHeight="660" MinWidth="880"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="FactVaultManager Hybrid" FontSize="26" FontWeight="SemiBold" />
            <TextBlock Text="C# production control with the existing Python engine" Margin="0,4,0,0" Opacity="0.7" />
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#404040" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Python production worker" FontWeight="SemiBold" />
                    <TextBlock x:Name="WorkerStatusText" Text="Connecting..." Margin="0,4,0,0" />
                    <TextBlock x:Name="RepositoryText" Margin="0,4,0,0" Opacity="0.7" />
                </StackPanel>
                <Button x:Name="RefreshProjectsButton" Grid.Column="1" Content="Refresh Projects" Padding="16,8" VerticalAlignment="Center" Click="RefreshProjects_Click" IsEnabled="False" />
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="#404040" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,14,0">
                    <TextBlock Text="Project" FontWeight="SemiBold" Margin="0,0,0,5" />
                    <ComboBox x:Name="ProjectComboBox" MinHeight="32" IsEnabled="False" DisplayMemberPath="DisplayName" SelectionChanged="ProjectComboBox_SelectionChanged" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,14,0">
                    <TextBlock x:Name="ProjectStatusText" Text="Loading projects..." FontWeight="SemiBold" />
                    <TextBlock x:Name="ProjectFolderText" Margin="0,5,0,0" Opacity="0.7" TextWrapping="Wrap" />
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="ProductionActionButton" Content="Produce Video" Padding="16,8" Margin="0,0,8,0" Click="ProductionAction_Click" IsEnabled="False" />
                    <Button x:Name="ResumeProductionButton" Content="Resume" Padding="14,8" Margin="0,0,8,0" Click="ResumeProduction_Click" IsEnabled="False" />
                    <Button x:Name="CancelProductionButton" Content="Cancel" Padding="14,8" Click="CancelProduction_Click" IsEnabled="False" />
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.ColumnSpan="3" Margin="0,10,0,0" Opacity="0.65"
                           Text="In Progress projects use Produce Video. Completed projects use Reproduce Video. Resume appears when a production checkpoint exists." />
            </Grid>
        </Border>

        <Border Grid.Row="3" BorderBrush="#404040" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="CurrentStageText" Text="Production ready" FontWeight="SemiBold" />
                    <ProgressBar x:Name="ProductionProgressBar" Height="10" Minimum="0" Maximum="100" Value="0" Margin="0,9,18,0" />
                </StackPanel>
                <TextBlock x:Name="ProductionProgressText" Grid.Column="1" Text="0%" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Margin="0,0,0,8">
                <TextBlock Text="Production log" FontWeight="SemiBold" DockPanel.Dock="Left" />
                <Button Content="Clear" Padding="12,5" DockPanel.Dock="Right" Click="ClearLog_Click" />
            </DockPanel>
            <TextBox x:Name="LogTextBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="13" Padding="10" />
        </Grid>
    </Grid>
</Window>
