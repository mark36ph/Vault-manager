name: Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install test dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest pytest-cov customtkinter

      - name: Run tests
        shell: pwsh
        run: |
          python -m pytest `
            --cov=project_manager `
            --cov=database `
            --cov-report=term-missing `
            --cov-fail-under=90